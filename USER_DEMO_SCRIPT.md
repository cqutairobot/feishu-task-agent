# 飞书群聊任务机器人完整演示与测试脚本

本文档用于把一个已经使用过的测试系统恢复成“首次使用”状态，并通过一组接近真实群聊的连续场景，向用户展示机器人已经实现的主要能力。

文档分为三部分：

1. 演示前清空数据库；
2. 演示账号、群聊和系统设置准备；
3. 可直接照着发送的完整群聊、私聊和管理后台演示流程。

> **重要：清空数据库会让系统忘记所有已初始化群、姓名绑定、管理员、任务、消息、提醒、设置和审计记录。** 下面两种清库方式都先保留备份。不要同时清理本地数据库和云端数据库，先确认本次演示实际使用哪一套环境。

---

## 0. 演示前必须确认：只运行一套机器人

如果本地 Mac 和阿里云服务器使用相同的飞书 App ID、App Secret，两边不能同时运行 Listener。否则可能出现：

- 同一条飞书消息被不同数据库分别处理；
- 重复创建任务或重复回复；
- 一边能看到任务，另一边看不到；
- 管理后台和飞书实际处理任务的数据库不是同一份。

本次使用本地 Mac 演示时，先在云服务器执行：

```bash
cd ~/feishu-task-agent-release
docker compose stop
```

本次使用阿里云 Docker 演示时，先回到本地运行 `python -u -m app dev` 的终端，按一次：

```text
Ctrl+C
```

看到 `All development services stopped.` 后再启动云端。

---

# 第一部分：把系统恢复成全新状态

只选择以下一种方式执行。

## 1. 本地 Mac、非 Docker 运行：快速清空数据库

这是日常开发和反复演示更常用的方式。它不会删除代码、`.env`、Python 虚拟环境或前端依赖，只会把当前 SQLite 数据库移入备份目录，然后创建一个全新空库。

### 1.1 停止本地程序

如果 `python -u -m app dev` 正在运行，在对应终端按一次：

```text
Ctrl+C
```

不要在程序仍写入数据库时直接移动数据库文件。

可在新的 Mac 终端检查管理端口是否仍被占用：

```bash
lsof -nP -iTCP:8000 -sTCP:LISTEN
lsof -nP -iTCP:3000 -sTCP:LISTEN
```

两个命令都没有输出，表示本地管理 API 和前端已经停止。如果仍有输出，先找到之前启动程序的终端正常停止，不要直接盲目使用 `kill -9`。

### 1.2 进入项目并核对数据库路径

以下路径是当前开发项目的位置：

```bash
cd /Users/Admin/Documents/ChatGPT/agent
pwd
test -f pyproject.toml && test -f .env && echo "project root: ok"
grep '^DATABASE_URL=' .env || echo "DATABASE_URL not set; using default"
```

当前默认结果应为：

```text
DATABASE_URL=sqlite:///data/feishu_task_agent.db
```

或者 `.env` 没有单独填写 `DATABASE_URL`，此时也会使用相同默认路径。

如果实际输出指向其他数据库文件，先停止，不要照搬下面命令；需要按实际路径调整。

### 1.3 把旧数据库移动到可恢复备份目录

在项目根目录执行：

```bash
DEMO_BACKUP_DIR="data/demo-backup-$(date +%Y%m%d-%H%M%S)"
mkdir -p "$DEMO_BACKUP_DIR"

for file in \
  data/feishu_task_agent.db \
  data/feishu_task_agent.db-wal \
  data/feishu_task_agent.db-shm \
  data/feishu_task_agent.db-journal
do
  if [ -e "$file" ]; then
    mv "$file" "$DEMO_BACKUP_DIR/"
  fi
done

echo "old database moved to: $DEMO_BACKUP_DIR"
ls -la "$DEMO_BACKUP_DIR"
```

这里使用 `mv` 而不是永久删除。需要恢复时，停止程序后可以从该目录找回旧数据库。

### 1.4 创建并检查全新空数据库

```bash
source .venv/bin/activate
python -m app db-status
```

预期所有业务计数都是 `0`：

```text
SQLite message store is ready.
chats: 0
users: 0
messages: 0
aliases: 0
detection_jobs: 0
detection_runs: 0
tasks: 0
task_materializations: 0
task_reminders: 0
```

### 1.5 启动本地完整程序

```bash
python -u -m app dev
```

看到以下内容后再开始飞书演示：

```text
All 6 development services are running. Press Ctrl+C once to stop all of them.
```

本地管理后台入口：

```text
http://127.0.0.1:3000
```

但正常登录仍应私聊机器人发送“管理后台”，使用机器人返回的一次性链接。

---

## 2. 阿里云、Docker Compose 运行：快速清空数据库

该方式只删除当前项目的 SQLite 命名卷。不会删除：

- GitHub 克隆的代码；
- `.env`；
- 已导入的三个项目镜像；
- Docker Engine；
- 服务器上的其他目录和普通文件。

### 2.1 进入云端项目并确认服务

登录服务器后执行：

```bash
cd ~/feishu-task-agent-release
pwd
docker compose ps --all
docker compose exec -T management-api python -m app db-status
```

确认当前目录是：

```text
/home/deploy/feishu-task-agent-release
```

### 2.2 先创建 SQLite 一致性备份

```bash
mkdir -p ~/feishu-task-agent-backups
./scripts/docker-backup.sh
```

只有看到以下内容才表示备份完成：

```text
backup_integrity: ok
backup_path: /home/deploy/feishu-task-agent-backups/...
backup_sha256: ...
```

可再次确认备份文件存在：

```bash
ls -lh ~/feishu-task-agent-backups
```

### 2.3 明确确认后删除唯一数据库卷

先确认目标卷名称：

```bash
docker volume inspect feishu-task-agent_task-data \
  --format '{{.Name}}'
```

预期只输出：

```text
feishu-task-agent_task-data
```

然后执行下面这段带确认的命令：

```bash
read -r -p 'This will reset ALL Feishu Task Agent data. Type RESET to continue: ' DEMO_RESET_CONFIRM

if [ "$DEMO_RESET_CONFIRM" = "RESET" ]; then
  docker compose down
  docker volume rm feishu-task-agent_task-data
else
  echo "reset cancelled; no data was changed"
fi
```

预期最后输出：

```text
feishu-task-agent_task-data
```

不要使用下面这些范围更大的命令：

```text
docker system prune --volumes
docker volume prune
```

它们可能影响服务器上的其他 Docker 项目。

### 2.4 用现有镜像重建空数据库并启动

```bash
docker compose up \
  -d \
  --no-build \
  --force-recreate \
  --wait \
  --wait-timeout 180
```

检查服务和空库：

```bash
docker compose ps --all
curl -fsS http://127.0.0.1:8080/health
echo
docker compose exec -T management-api python -m app db-status
```

预期：

- `migrate` 为 `Exited (0)`；
- 其余七个服务为 `Up` 或 `healthy`；
- 健康检查返回 `{"status": "ok", ...}`；
- `db-status` 中所有业务计数为 `0`。

查看这台服务器实际配置的公网管理入口：

```bash
grep '^DEPLOY_PUBLIC_URL=' .env
```

用浏览器打开输出的地址。如果以后服务器公网 IP 发生变化，应先更新 `.env` 中的
`DEPLOY_PUBLIC_URL` 并重建容器，不能继续使用旧地址。

---

# 第二部分：演示准备

## 3. 推荐演示人员

完整展示最好准备 4 个飞书账号：

| 演示角色 | 本文示例姓名 | 主要操作 |
| --- | --- | --- |
| 飞书群主、导师、群管理员 | 林老师 | 初始化群、分配任务、进入后台、审核和纠错 |
| 成员 A | 王政 | 接收任务、完成、延期、使用卡片 |
| 成员 B | 李明 | 接收任务、取消、共同负责 |
| 成员 C | 周悦 | 接收无截止时间任务、多人任务 |

如果实际账号姓名不同，请把后文“林老师、王政、李明、周悦”统一替换成真实测试姓名。

至少需要 3 个账号才能展示管理员、个人任务和共同任务。只有 2 个账号时，可以让林老师同时承担周悦的任务，但多人场景的展示效果会弱一些。

## 4. 演示群准备

建议新建一个专用群，例如：

```text
飞书任务机器人完整演示群
```

把以下对象加入群：

- 机器人；
- 林老师；
- 王政；
- 李明；
- 周悦。

确保林老师是当前飞书群主。

## 5. 发送脚本时的规则

1. 只有标记为“群聊发送”或“私聊发送”的代码块需要真正发送。
2. `@机器人` 必须使用飞书真正的 `@` 选择机器人。
3. `@王政` 等成员提及，如果场景要求测试飞书提及，也必须使用真正的 `@成员`。
4. 普通任务对话不需要 `@机器人`。
5. 同一个多轮场景中的消息最好连续发送，每两条间隔少于 20 秒。
6. 一个场景发送完后不要马上进入下一个场景，等待约 60～120 秒并确认任务结果。
7. 模型生成的任务标题可能与本文“预期标题”措辞不同，只要负责人、事项、截止时间和任务拆分关系正确即可。
8. 任务编号由数据库动态生成。后文的 `<编号A>`、`<编号B>` 必须替换成机器人真实返回的编号，不能原样发送尖括号占位符。

## 6. 初始化与姓名绑定

### 6.1 群主初始化

林老师在群聊发送：

```text
@机器人 初始化本群
```

预期机器人群内回复：

```text
✅ 本群任务管理已初始化成功

你已成为本群任务管理员。现在可以私聊机器人发送“管理后台”。
```

### 6.2 每位成员绑定姓名

王政使用自己的账号在群聊发送：

```text
@机器人 绑定姓名：王政
```

李明使用自己的账号发送：

```text
@机器人 绑定姓名：李明
```

周悦使用自己的账号发送：

```text
@机器人 绑定姓名：周悦
```

林老师也建议绑定：

```text
@机器人 绑定姓名：林老师
```

每个人查询自己的绑定结果：

```text
@机器人 我的姓名
```

### 6.3 可选：演示重新绑定会释放旧姓名

王政发送：

```text
@机器人 绑定姓名：王小政
```

确认成功后再改回：

```text
@机器人 绑定姓名：王政
```

此时“王小政”会被释放，其他当前群成员可以使用；王政在本群只保留“王政”这一个任务姓名。

## 7. 登录后台并确认默认设置

林老师私聊机器人发送：

```text
管理后台
```

打开机器人返回的 5 分钟内有效的一次性链接，确认当前群的默认设置：

- 自动任务识别：开启；
- 自动进入待办的置信度：85%；
- 任务识别范围：宽泛任务；
- 四阶段负责人提醒：开启；
- 时区：Asia/Shanghai；
- 管理员通知：全部管理员。

设置确认后回到飞书群，开始正式聊天演示。

---

# 第三部分：完整聊天演示

## 8. 场景一：跨多句话理解负责人、事项和截止时间

该场景证明机器人不是只看最后一句话。

### 群聊发送

林老师：

```text
中午食堂是不是又换菜单了？我看今天排队的人挺多。
```

李明：

```text
好像新增了一个窗口，味道还可以。
```

林老师：

```text
对了，ResNet50 的 baseline 还缺一组不同随机种子的结果，表格现在空着一行。
```

王政：

```text
我来补吧，训练脚本我这边已经能跑了。
```

林老师：

```text
好，那明天下午 6 点前跑完，把均值、方差和日志路径一起放到实验服务器，再在群里说一声。
```

### 预期结果

机器人结合后三句话创建 1 个任务：

| 项目 | 预期 |
| --- | --- |
| 负责人 | 王政 |
| 事项 | 补充 ResNet50 baseline 不同随机种子结果，并上传统计值和日志路径 |
| 截止时间 | 明天 18:00 |
| 是否需要王政再次确认 | 不需要 |

这个场景也用于验证“迟到的截止时间补全”。即使故意让“我来补吧”和最后一句之间间隔
超过 20 秒，使系统第一轮先创建了一个无截止时间任务，最后一句被识别后也应更新同一
任务并保留原编号，最终任务清单仍然只能有 1 条 ResNet50 任务。不得同时出现一条“未
设置截止时间”和一条“明天 18:00”的重复记录。

王政应收到新任务私聊。把实际编号记为：

```text
编号A：________________
```

## 9. 场景二：一句明确指令直接创建，不要求负责人回复

### 群聊发送

林老师：

```text
李明，请在后天下午 5 点前完成星河项目数据字典初稿，至少包含用户表、任务表和通知表的字段说明，并把文档链接发到群里。
```

李明不用回复“收到”。

### 预期结果

创建 1 个正式任务：负责人李明，截止后天 17:00。李明收到私聊通知。

```text
编号B：________________
```

## 10. 场景三：一句聊天记录下达多条任务

### 群聊发送

林老师：

```text
下一轮演示这样安排：王政明天下午 4 点前完成登录页移动端适配；李明后天下午 6 点前补齐接口错误码文档；周悦下周一上午 10 点前整理 8 位测试用户的访谈纪要。三项工作分别提交，不用等其他人。
```

### 预期结果

一条消息拆成 3 个独立任务：

| 记录 | 负责人 | 事项 |
| --- | --- | --- |
| 编号C | 王政 | 登录页移动端适配 |
| 编号D | 李明 | 接口错误码文档 |
| 编号E | 周悦 | 8 位用户访谈纪要 |

```text
编号C：________________
编号D：________________
编号E：________________
```

三位负责人应分别收到自己的新任务通知。

## 11. 场景四：共同任务与各自任务同时出现

### 群聊发送

林老师：

```text
王政和李明共同负责前后端登录联调，后天下午 3 点前只交一份联合联调报告，两个人都要对最终结果负责。另外两个人各自做一次独立压测：王政负责前端首屏性能，李明负责登录接口并发，明晚 9 点前每人各交一份自己的结果，不要合并。
```

### 预期结果

应创建 3 个任务，而不是 2 个或 4 个：

1. 一个共同任务：王政和李明共同负责，只有一个任务编号；
2. 王政的前端首屏独立压测；
3. 李明的登录接口并发独立压测。

```text
编号F（共同联调）：________________
编号G（王政压测）：________________
编号H（李明压测）：________________
```

共同任务的两位负责人都应收到通知，并且在各自任务列表中看到相同编号F。

## 12. 场景五：长会议纪要，夹杂闲聊、背景、已完成事项和未确定讨论

下面整段由林老师作为一条长消息发送。不要拆成多条。

### 群聊发送

```text
星河智能体项目周会纪要

会议时间：今天 14:00—15:35
参会人：林老师、王政、李明、周悦
记录人：周悦

一、会前闲聊和近况
最近天气比较闷，实验室空调下午有点热，大家讨论了下周要不要把例会从三楼换到五楼。王政提到食堂二楼新开了窗口，李明说上周订的显示器已经送到。以上都只是日常交流，不涉及项目安排。

二、上周工作回顾
1. 旧服务器的数据迁移已经完成，校验结果正常，这项工作到此结束，不需要再创建任务。
2. 第一版用户画像表已经整理完成并发给大家，周悦昨天也已经把访谈录音归档。
3. 原计划制作纸质宣传册，但会议决定取消该计划，任何人都不需要继续准备宣传册。

三、方案讨论
大家比较了本地规则、轻量模型和大模型三种任务抽取方式。当前倾向继续使用大模型结合上下文，但成本是否要进一步下降还没有结论。有人提出以后也许可以增加语音转写，不过本轮不做，暂时没有负责人和截止时间。关于是否将管理后台改成深色模式也只是建议，本次会议没有立项。

四、本周明确行动项，以本节为准
1. 王政负责完成管理后台任务详情页的移动端原型，要求能展示任务编号、负责人、截止时间、状态和证据消息，明天下午 6 点前把可点击原型链接发到群里。
2. 李明负责检查最近三批测试数据的空值、重复值和异常日期，形成一份数据质量报告，后天下午 5 点前上传到飞书文档。
3. 周悦负责整理用户访谈问题池，并给每个问题标注“必须问、可选问、追问”三个类别。先创建任务，但暂时不设置截止时间，等样本名单确认后再补日期。
4. 王政和周悦共同负责下一轮答辩演示方案，下周一晚上 8 点前只提交一份共同版本，内容包括 10 分钟演示流程、失败时的备用方案和现场分工，两个人共同对交付结果负责。

五、尚未形成任务的事项
直播宣传是否要做，等学院通知后再说；新的 Logo 颜色大家还没有达成一致；聚餐地点下次再讨论。这些事项现在都没有明确执行要求，不应登记任务。

六、下次会议
下次例会暂定下周二下午，具体会议室稍后再确认。今天的会议到这里，感谢大家。
```

### 预期结果

机器人应从长纪要中提取第“四”部分的 4 项明确任务：

| 记录  | 负责人   | 事项         | 截止时间      |
| --- | ----- | ---------- | --------- |
| 编号I | 王政    | 任务详情页移动端原型 | 明天 18:00  |
| 编号J | 李明    | 三批测试数据质量报告 | 后天 17:00  |
| 编号K | 周悦    | 用户访谈问题池分类  | 无截止时间     |
| 编号L | 王政、周悦 | 下一轮答辩演示方案  | 下周一 20:00 |

```text
编号I：________________
编号J：________________
编号K：________________
编号L：________________
```

以下内容不应创建新任务：

- 食堂、空调、会议室和显示器闲聊；
- 已经完成的服务器迁移和录音归档；
- 已明确取消的宣传册；
- 没有立项的语音转写、深色模式和直播宣传；
- 没有负责人、没有执行要求的 Logo 和聚餐讨论。

如果模型标题与表格略有差异，但四个任务的负责人、共同关系、期限和无期限状态正确，即可判定通过。

## 13. 场景六：只有询问、闲聊和模糊讨论，不应创建任务

以下消息可以连续发送：

### 群聊发送

王政：

```text
今天实验结果出来了吗？
```

李明：

```text
我还在看日志，暂时只是想确认一下昨天的显存峰值是多少。
```

周悦：

```text
下周也许可以看看要不要换一个图表配色，不过现在还没决定。
```

林老师：

```text
先讨论到这里，今天没有新增安排。晚上降温，大家回去注意安全。
```

### 预期结果

等待识别窗口结束后，任务数量不应增加。

## 14. 场景七：无截止时间任务与负责人后续补日期

编号K本来没有截止时间。先让周悦私聊机器人查询：

### 周悦私聊发送

```text
任务列表
```

找到“用户访谈问题池分类”对应的真实编号。如果与记录的编号K一致，继续发送：

```text
<编号K> 截止时间改为下周三下午 6 点
```

例如实际编号是 `T-9Q`，应发送：

```text
T-9Q 截止时间改为下周三下午 6 点
```

### 预期结果

- 机器人确认任务已延期或截止时间已更新；
- 任务仍为待办；
- 新截止时间为下周三 18:00；
- 对应的新提醒计划被创建；
- 管理员收到私聊变更通知。

> 默认无截止时间任务会在创建 24 小时后提醒负责人、72 小时后升级管理员。完整演示不必真的等待三天；后台“群设置”页面可以展示这两项规则和审计记录。

## 15. 场景八：查看个人任务与管理员全群任务

### 15.1 普通成员在群里查询

王政在群聊发送：

```text
@机器人 任务列表
```

预期只看到王政在当前群负责的未完成任务，包括与他人共同负责的任务。

### 15.2 普通成员私聊查询

王政私聊机器人发送：

```text
任务列表
```

预期收到个人任务卡片或文字列表。正式待办通常带“完成、延期、取消”操作。

### 15.3 管理员在群里查询

林老师在群聊发送：

```text
@机器人 本群任务
```

预期管理员看到当前群所有人的未完成任务，而不是只看到林老师自己的任务。

### 15.4 编号长度说明

任务编号可能是 `T-1A`、`T-9Q`，任务增多后会自动增长，不会永远只有两位。私聊操作时既可以使用完整编号，也可以在无歧义时使用去掉 `T-` 的短编号。

## 16. 场景九：私聊自然语言完成、延期和取消

每次私聊只操作一个任务编号。

### 16.1 王政完成任务

王政私聊：

```text
<编号A> 已经完成了，baseline 的结果、均值和日志路径都放到服务器了
```

预期：编号A变为 `done`，未发送提醒被取消，管理员收到完成通知。

### 16.2 李明延期任务

李明私聊：

```text
<编号B> 数据字典还需要补两个字段，延期到下周二下午 6 点
```

预期：编号B保持待办，新截止时间为下周二 18:00，旧提醒取消并按新日期重建，管理员收到延期通知。

### 16.3 李明取消任务

李明选择另一个自己负责、确定不再需要的测试任务，例如编号H，私聊：

```text
<编号H> 取消，这次压测需求已经撤销，不再执行
```

预期：编号H变为 `cancelled`，未发送提醒取消，管理员收到取消通知。

### 16.4 模糊表达不应误完成

选择一个仍在进行中的编号，私聊：

```text
<某个未完成编号> 快做完了，还差最后一点
```

预期不应把任务直接改成已完成。

### 16.5 一条消息包含两个编号会被拒绝

可选安全性演示：

```text
<编号C> 已完成，<编号D> 取消
```

预期机器人拒绝或提示一次只能操作一个有效任务编号。随后应拆成两条消息分别处理。

## 17. 场景十：使用私聊交互式卡片

王政私聊发送：

```text
任务列表
```

在一个仍为待办的任务卡片上演示以下任一操作：

1. 点击“延期”；
2. 选择新的日期和时间；
3. 确认；
4. 再次发送“任务列表”，验证卡片已更新。

也可以选择一个专门用于演示的任务点击“完成”。

预期：

- 卡片操作成功；
- 重复点击旧卡片不会重复修改任务；
- 管理后台生命周期记录中能看到操作来源；
- 管理员收到对应私聊通知。

## 18. 场景十一：管理员私聊纠错

以下操作必须由当前群任务管理员林老师私聊机器人完成。

### 18.1 修改标题

```text
<编号C> 标题改为“完成登录页手机和平板双端适配”
```

### 18.2 调整负责人

```text
<编号D> 负责人改为李明和王政共同负责
```

### 18.3 撤销误识别

选择一个专门用于撤销演示且不再需要的任务：

```text
<目标编号> 这是误识别的，不是任务，请撤销
```

### 预期结果

- 普通负责人不能越权修改任务标题或替换负责人；
- 管理员可以执行并收到确认；
- 新负责人会收到任务分配通知；
- 被移除负责人会收到变更通知；
- 误识别撤销后任务不再作为有效待办展示；
- 后台保留完整纠错审计，不会物理删除历史证据。

## 19. 场景十二：默认宽泛模式与仅工作/科研模式

### 19.1 默认宽泛模式识别生活需求

确认后台“任务识别范围”为“宽泛任务”，然后群聊发送：

```text
李明，请在周六晚上 7 点前订好下周组会后的聚餐位置，确认能坐 8 个人，并把地址和预订信息发群里。
```

预期：生活安排也会创建为李明的任务。

```text
编号M：________________
```

### 19.2 切换为仅工作/科研

林老师进入管理后台“群设置”：

1. 将任务识别范围改为“仅工作 / 科研”；
2. 保存；
3. 回到群里发送：

```text
王政，请在周日下午 4 点前帮大家预订羽毛球场，订好后把场地号发群里。
```

预期：该生活安排不创建任务。

再发送工作任务：

```text
王政，请在周日下午 6 点前整理本周模型评测的准确率、召回率和失败案例，输出一页总结。
```

预期：工作任务正常创建。

演示结束后把任务识别范围恢复为“宽泛任务”，因为这是系统默认和日常推荐模式。

## 20. 场景十三：低置信度任务进入管理员待审核

该场景用于展示“模型识别”和“正式通知”之间还有安全门槛。

### 准备

林老师在管理后台“群设置”中：

1. 确认自动任务识别开启；
2. 临时把“自动进入待办的置信度”提高到 100%；
3. 保存。

### 群聊发送

```text
周悦，请在下周一下午 5 点前整理一份新用户引导页改版建议，包含当前问题、三个参考案例和推荐方案。
```

### 预期结果

多数情况下，该识别结果会因为低于 100% 门槛进入 `pending`：

- 周悦暂时不收到正式新任务通知；
- 管理后台“待审核”中出现该记录；
- 管理员打开详情可以查看证据消息、负责人、期限和模型置信度。

林老师在后台点击“确认任务”。确认后：

- 状态转为 `todo`；
- 周悦收到正式新任务通知；
- 截止提醒开始生效。

演示完成后务必把门槛恢复为 85%。

> 模型输出存在概率性。如果模型恰好返回 100%，该任务可能直接进入待办。此时换一句稍有条件但仍可执行的表达重新测试，例如：“周悦先看看引导页，如果现有问题确认存在，就在下周一前整理一页改版建议”。

## 21. 场景十四：暂停自动识别，但消息仍会保存

### 21.1 关闭识别

林老师进入后台“群设置”：

1. 关闭“自动任务识别”；
2. 保存；
3. 观察置信度和任务范围控件变为不可编辑。

群聊发送：

```text
王政，请在明天下午 3 点前完成暂停识别演示记录，并把截图发到群里。
```

等待后预期：不创建任务，也不发送新任务通知。

### 21.2 重新开启识别

后台重新开启“自动任务识别”并保存。

注意：暂停期间的旧消息不会被自动补识别。重新发送一条新的明确指令：

```text
王政，请在明天下午 3 点前完成恢复识别后的验收记录，并把截图发到群里。
```

预期：新消息正常创建任务。

```text
编号N：________________
```

## 22. 场景十五：管理后台完整展示

林老师私聊机器人：

```text
管理后台
```

使用新的一次性链接登录后，按顺序向用户展示：

### 22.1 任务总览

- 未完成任务数量；
- 7 天内截止数量；
- 已逾期数量；
- 缺少截止时间数量；
- 当前群成员和管理员数量。

### 22.2 筛选、搜索和分页

1. 切换“未完成、待审核、待办、逾期、全部”；
2. 输入一个真实任务编号搜索；
3. 输入“baseline”或“数据质量”搜索标题和说明；
4. 说明每页 10 条，任务增多后使用上一页、下一页查看。

### 22.3 查看任务详情

打开长会议纪要生成的任一任务，展示：

- 来源群；
- 负责人；
- 状态和截止时间；
- 证据消息；
- 创建和生命周期记录；
- 新任务通知、提醒或管理员通知的投递记录。

### 22.4 手动创建任务

点击“新建任务”，创建一条仅用于演示的任务：

| 字段 | 示例 |
| --- | --- |
| 标题 | 手动补建演示验收报告 |
| 说明 | 汇总本次机器人演示截图和结论 |
| 负责人 | 王政、李明 |
| 截止时间 | 后天下午 18:00 |

保存后两位负责人都应收到通知。

### 22.5 编辑、完成、取消和恢复

在后台对演示任务依次展示：

1. 修改标题；
2. 修改负责人；
3. 修改截止时间；
4. 标记完成；
5. 从历史任务中恢复已完成任务；
6. 取消；
7. 再恢复已取消任务。

每一步都检查生命周期和通知记录。

### 22.6 合并重复任务

如果演示期间出现两条实际含义重复的任务，可在后台合并：

- 选择保留的目标任务；
- 把重复任务合并进去；
- 原任务变为 `merged` 历史记录；
- 证据和来源被保留；
- 重复任务未发送的提醒被取消。

## 23. 场景十六：管理员管理与通知范围

在后台“群管理员”页面：

1. 把王政添加为第二位群任务管理员；
2. 刷新页面，确认林老师和王政都存在；
3. 在“群设置”将管理员通知模式切换为“指定管理员”；
4. 只选择林老师并保存；
5. 让李明完成或延期一个任务；
6. 确认只有林老师收到管理员通知；
7. 再改回“全部管理员”；
8. 让另一个负责人变更任务，确认两位有效管理员按去重规则收到通知。

还可以展示：

- 系统不允许删除最后一名管理员；
- 成员离群后管理员权限会被撤销；
- 当前飞书群主可以通过 `@机器人 接管本群` 恢复任务管理员权限。

如果不希望改变演示群长期权限，场景结束后可撤销王政管理员，只保留林老师。

## 24. 场景十七：快速展示四阶段截止提醒（可选）

默认提醒是截止前 72 小时、24 小时、截止当天和逾期后，不适合在十几分钟演示中全部等待。项目保留了专门的本地验收模式，可以把四个阶段压缩到分钟级。

> 只在专用演示数据库和测试群使用。演示结束必须恢复 `false`。不要在有真实任务的正式云端长期开启。

### 24.1 本地 Mac 开启快速提醒模式

停止 `python -u -m app dev`，编辑项目根目录 `.env`：

```dotenv
REMINDER_TEST_MODE=true
REMINDER_WORKER_POLL_SECONDS=1
```

重新启动：

```bash
cd /Users/Admin/Documents/ChatGPT/agent
source .venv/bin/activate
python -u -m app dev
```

### 24.2 创建一个 12 分钟后截止的任务

先看当前北京时间，计算 12 分钟后的具体时刻。例如现在是 15:00，就把下面的时间改成 15:12。

林老师群聊发送：

```text
王政，请在今天 15:12 前完成四阶段提醒演示记录，并把收到的提醒截图整理好。
```

不要原样使用 `15:12`，必须改成演示当天“当前时间 + 12 分钟”。

快速模式相对于任务截止时间的四阶段为：

| 阶段 | 发送时刻 |
| --- | --- |
| 第一次提醒 | 截止前 6 分钟 |
| 第二次提醒 | 截止前 4 分钟 |
| 截止提醒 | 截止前 2 分钟 |
| 逾期提醒 | 截止后 1 分钟 |

考虑到任务识别本身需要约 20 秒防抖和模型处理时间，使用 12 分钟而不是 8 分钟可以避免错过第一阶段。

### 24.3 演示结束恢复正式模式

停止本地程序，把 `.env` 改回：

```dotenv
REMINDER_TEST_MODE=false
REMINDER_WORKER_POLL_SECONDS=1
```

然后重新启动 `python -u -m app dev`。

正式模式下仍以后台当前群设置的真实小时和分钟规则为准。

## 25. 场景十八：多群数据隔离（可选）

准备第二个飞书群，例如“飞书任务机器人第二演示群”，把机器人和部分成员加入。

第二群当前群主发送：

```text
@机器人 初始化本群
```

成员在第二群重新绑定姓名，因为姓名绑定按群隔离：

```text
@机器人 绑定姓名：王政
```

在第二群创建一项独有任务：

```text
王政，请在后天下午 4 点前完成第二演示群的数据隔离验收记录。
```

预期：

- 第二群任务不会出现在第一群普通成员的群内任务查询中；
- 第一群管理员如果不是第二群管理员，后台不能切换到第二群；
- 同一个人可以在不同群绑定不同任务姓名；
- 第二群改名不会改变任务的 `chat_id` 隔离关系；
- 后台和私聊查询始终按授权范围展示，不会串群。

---

# 第四部分：演示验收清单

## 26. 核心功能验收表

| 编号 | 功能 | 通过标准 |
| --- | --- | --- |
| 1 | 全新系统初始化 | 清库后计数为 0，群主能初始化 |
| 2 | 姓名绑定 | 每人一个姓名，重新绑定释放旧姓名 |
| 3 | 无需 @ 识别 | 普通任务聊天能进入识别 |
| 4 | 多轮上下文 | “事项—认领—期限”三句话合成正确任务 |
| 5 | 直接分配 | 不要求负责人回复也能创建 |
| 6 | 一句话多任务 | 三个独立事项拆成三条任务 |
| 7 | 多人共同任务 | 共同交付只有一个编号、多个负责人 |
| 8 | 各自任务 | 每人独立交付时分别建任务 |
| 9 | 长会议纪要 | 从长文本中提取明确行动项，过滤闲聊和已取消内容 |
| 10 | 非任务过滤 | 询问、建议和闲聊不新增任务 |
| 11 | 无截止时间 | 能建任务，后续能由负责人补日期 |
| 12 | 新任务通知 | 正式任务私聊对应负责人 |
| 13 | 个人与管理员查询 | 普通成员看自己，管理员看本群全部 |
| 14 | 自然语言生命周期 | 完成、延期、取消正确写入 |
| 15 | 卡片交互 | 完成、延期、取消按钮可用且幂等 |
| 16 | 管理员纠错 | 标题、负责人和误识别撤销可审计 |
| 17 | 宽泛/工作模式 | 生活任务是否识别符合当前设置 |
| 18 | 置信度审核 | 低于门槛先 pending，确认后才通知 |
| 19 | 暂停识别 | 消息保存但不建任务，恢复后新消息正常 |
| 20 | 管理后台 | 搜索、分页、详情、创建、编辑、恢复和合并可用 |
| 21 | 管理员策略 | 多管理员和指定通知对象生效 |
| 22 | 截止提醒 | 正式或快速模式按阶段私聊负责人 |
| 23 | 多群隔离 | 任务、姓名、管理员和设置不串群 |

## 27. 建议的展示顺序和时间

### 30 分钟核心展示

1. 清空数据库并启动：5 分钟；
2. 初始化和姓名绑定：3 分钟；
3. 场景一、三、五：8～12 分钟；
4. 私聊任务列表、自然语言完成和卡片：5 分钟；
5. 管理后台搜索、详情、审核和设置：5～8 分钟。

### 60～90 分钟完整验收

按第 8～25 节依次执行，并在每个识别场景之间等待结果。快速提醒场景本身还需要约 15 分钟。

## 28. 演示中常见现象

### 28.1 为什么任务不是立刻出现

机器人默认等待群聊约 20 秒，观察是否还有后续上下文，然后才进入模型识别。模型调用、队列和通知还需要额外时间。演示时不要在刚发送后几秒就判断失败。

### 28.2 为什么标题和预期文字不完全一样

标题和说明由模型概括，不要求逐字一致。应重点核对：

- 是否真的是任务；
- 负责人是否正确；
- 多人关系是否正确；
- 截止时间是否正确；
- 证据消息是否来自当前场景。

### 28.3 为什么负责人没收到新任务通知

依次检查：

1. 是否进入 `pending` 而不是 `todo`；
2. 姓名是否在当前群正确绑定；
3. 负责人是否仍在群内；
4. 飞书应用是否仍有向成员发送消息的权限；
5. Notification Worker 是否健康；
6. 本地和云端是否误开了两套机器人。

### 28.4 为什么私聊修改失败

检查：

- 是否使用真实任务编号；
- 一条消息是否只包含一个编号；
- 操作者是否是负责人或本群管理员；
- 任务是否仍为可操作状态；
- 延期是否给出了明确的新日期和时间。

### 28.5 演示结束后是否需要再次清库

如果下一批用户需要看到真正的首次初始化流程，可以再次执行本文第 1 节或第 2 节。若需要保留本次展示记录，就不要清库，直接使用后台继续管理。

---

## 29. 最短演示话术

向用户介绍时，可以这样概括：

> 大家仍然按原来的方式在飞书群里聊天，不需要专门 @ 机器人。机器人会结合最近一段上下文，找出谁需要完成什么、什么时候完成，并为任务分配稳定编号。负责人会收到私聊通知和截止提醒，可以直接用自然语言或卡片完成、延期、取消。低置信度结果先交给管理员审核，管理员还能在网页后台纠错、搜索和维护任务。所有姓名、任务、管理员和设置都按群隔离。

更完整的日常操作说明请参阅 [《飞书群聊任务机器人用户使用手册》](USER_GUIDE.md)。
