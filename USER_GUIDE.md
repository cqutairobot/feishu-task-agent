# 飞书群聊任务机器人用户使用手册

本手册面向使用机器人的普通群成员、任务负责人、飞书群主和群任务管理员，介绍机器人已经实现的功能、日常操作方法、权限规则和常见问题。文档不涉及服务器部署、代码开发或运维操作。

> 当前系统固定使用中国标准时间（Asia/Shanghai，UTC+8）。下文中的日期和时间均按北京时间理解。

## 快速导航

| 如果你想做什么 | 建议阅读 |
| --- | --- |
| 第一次在新群启用机器人 | 第 3 节“新群首次启用” |
| 绑定真实姓名或解决姓名冲突 | 第 4 节“姓名绑定的完整规则” |
| 了解机器人会识别哪些聊天 | 第 5～6 节“机器人怎样识别任务”“置信度与待审核任务” |
| 查看、完成、延期或取消自己的任务 | 第 9～11 节“查看任务列表”“私聊自然语言维护任务”“私聊任务卡片” |
| 了解截止提醒和无截止时间追问 | 第 12～13 节 |
| 进入管理后台维护全群任务 | 第 14～18 节 |
| 在多个群中使用机器人 | 第 20 节“多群使用与数据隔离” |
| 快速查找所有聊天命令 | 第 22 节“完整命令速查表” |
| 遇到机器人不回复或任务未识别 | 第 23 节“常见问题与排查” |
| 只记住最常用的几个操作 | 第 26 节“最简使用记忆” |

## 1. 机器人能做什么

机器人用于把自然发生在飞书群里的任务安排，自动转成可以跟踪、提醒和维护的任务记录。

一个典型流程如下：

1. 群成员照常聊天，不需要为了任务识别专门 `@机器人`。
2. 机器人结合最近一段群聊，而不是只看最后一句话。
3. 当聊天中已经出现明确的负责人和可执行事项时，机器人创建任务。
4. 每个任务获得一个稳定编号，例如 `T-1A`。
5. 正式任务创建后，机器人私聊每位负责人。
6. 临近截止时间时，机器人自动提醒负责人；逾期后也会通知管理员。
7. 负责人可以私聊机器人，用自然语言完成、取消或延期任务。
8. 管理员可以在网页后台审核、纠错、补建、搜索和管理任务。

当前已实现的核心能力包括：

- 无需 `@机器人` 读取已初始化群的普通文本消息。
- 结合最多最近 30 条、总计不超过约 12,000 个字符的群聊上下文识别任务。
- 支持跨多句话理解负责人、事项和截止时间。
- 支持一句话分配多条不同任务。
- 支持多人共同负责同一项任务。
- 支持多人各自完成不同或独立交付的任务。
- 支持工作、科研、会议、学习和生活需求两种识别范围。
- 支持通过绑定姓名或飞书 `@成员` 精确关联负责人。
- 支持有截止时间和暂时没有截止时间的任务。
- 支持低置信度任务进入管理员待审核，不直接打扰负责人。
- 支持新任务通知、四阶段截止提醒、无截止时间追问和管理员通知。
- 支持私聊任务列表和交互式任务卡片。
- 支持自然语言完成、延期、取消和管理员纠错。
- 支持管理后台、分群管理员、任务审计、搜索和分页。
- 所有任务、姓名、管理员和设置都按群隔离。

## 2. 使用角色与权限

同一个人在不同群中可以拥有不同角色。

| 角色 | 可以做什么 |
| --- | --- |
| 普通群成员 | 在本群绑定任务姓名；查看自己负责的任务；操作自己负责的正式任务 |
| 任务负责人 | 接收新任务和截止提醒；私聊完成、延期或取消任务；使用任务卡片 |
| 群任务管理员 | 查看本群全部任务；审核低置信度任务；手动创建和纠错任务；管理成员权限和群设置 |
| 当前飞书群主 | 首次初始化本群；在管理员权限丢失时接管本群；初始化后也拥有管理员能力 |
| 身份管理员（可选） | 使用特殊群命令替其他成员绑定姓名；此角色由系统部署方配置，不等同于普通群管理员 |

需要特别注意：

- 飞书群主和“群任务管理员”是两个概念。群主可通过初始化或接管成为任务管理员。
- 可以设置多名群任务管理员。
- 每个群至少保留一名任务管理员。
- 普通管理员可以在后台授予或撤销其他当前群成员的管理员权限。
- 当前群主即使曾被撤销任务管理员，也可在群里发送“接管本群”恢复权限。

## 3. 新群首次启用

### 3.1 把机器人加入群聊

先确认机器人已经被加入目标飞书群，并且应用已经由部署方正常发布和运行。

### 3.2 群主初始化本群

当前飞书群主在群里发送：

```text
@机器人 初始化本群
```

这里的 `@机器人` 必须使用飞书真正的 `@` 功能选择机器人，不能只手工输入机器人名称。

成功后机器人会回复：

```text
✅ 本群任务管理已初始化成功

你已成为本群任务管理员。现在可以私聊机器人发送“管理后台”。
```

初始化前，机器人不会把该群的普通聊天当作可管理任务群。初始化是每个新群都要单独完成的一次性操作。

如果本群已经初始化，原管理员不可用或群主发生变化，当前飞书群主可发送：

```text
@机器人 接管本群
```

机器人会实时向飞书核验当前群主，只有真正的当前群主才能执行。

### 3.3 成员绑定任务姓名

每位可能成为任务负责人的成员，建议在群里发送：

```text
@机器人 绑定姓名：王政
```

也支持：

```text
@机器人 我的姓名：王政
@机器人 我的姓名是王政
```

查询自己在当前群绑定的姓名：

```text
@机器人 我的姓名
```

姓名绑定成功后，群里出现“王政完成前端页面”时，机器人就能把“王政”精确对应到该成员的飞书 Open ID，从而私聊正确的人。

姓名绑定必须在目标群里完成，当前不支持在私聊里绑定姓名。

## 4. 姓名绑定的完整规则

### 4.1 一个成员在一个群只能有一个任务姓名

同一成员再次绑定新姓名时，会直接替换原姓名：

```text
@机器人 绑定姓名：王哈
```

替换成功后：

- 新任务使用“王哈”关联该成员；
- 原来的“王政”立即释放；
- 本群其他成员可以重新绑定“王政”；
- 已经创建的历史任务仍保留创建时的负责人姓名快照和审计记录。

### 4.2 一个姓名在一个群只能属于一个成员

如果“王政”已经被本群另一名成员占用，机器人会拒绝重复绑定。需要原成员先改名，或者由有权限的身份管理员处理。

### 4.3 姓名按群隔离

同一个姓名可以在不同群中分别绑定，不会串群。例如：

- A 群的“王政”可以对应成员甲；
- B 群的“王政”可以对应成员乙；
- 任务识别和提醒始终使用任务来源群里的绑定关系。

### 4.4 成员离群后的处理

系统同步到成员已经离开群聊后，会：

- 释放该成员在本群占用的任务姓名；
- 自动撤销该成员在本群的管理员权限；
- 保留已有任务和历史审计，不物理删除记录。

因此，新加入的成员可以在同步完成后绑定离群成员以前使用的姓名。

### 4.5 飞书显示名与任务姓名

飞书显示名可能是“飞书用户0976B”，而导师在聊天中使用的真实姓名可能是“王政”。绑定姓名就是为了解决这类差异。

管理后台的成员页面会同时显示：

- 飞书名称；
- 任务姓名；
- 是否为群主；
- 是否为群任务管理员。

## 5. 机器人怎样识别任务

### 5.1 普通聊天不需要 `@机器人`

完成本群初始化后，成员直接在群里说：

```text
王政，请在周五前完成登录页面开发。
```

即可触发任务识别，不需要再 `@机器人`。

只有“初始化本群”“绑定姓名”“任务列表”等机器人命令需要显式 `@机器人`。

### 5.2 一句明确指令不需要负责人确认

下面这句话已经足够形成任务：

```text
王政，请在 8 月 30 日 18:00 前完成前端页面开发。
```

不要求王政再回复“收到”或“好的”。机器人不会把确认回复当作创建任务的必要条件。

### 5.3 机器人会结合上下文

例如：

```text
导师：这个实验还缺一个 baseline。
张三：我来补吧。
导师：行，周四之前跑出来。
```

机器人会综合最近对话，理解为张三需要在周四前完成 baseline 实验，而不是把最后一句孤立处理。

### 5.4 二十秒收集窗口

每次出现新的普通群文本消息后，系统默认等待约 20 秒再开始识别。如果这 20 秒内又有新消息，等待时间会从最新消息重新计算。

这样可以把连续表达的上下文收集完整。二十秒只是“等待更多消息”的防抖窗口，不代表同一时间段的消息一定属于同一个任务。模型仍会根据负责人、交付内容、时间和证据分别判断。

因此，正常的新任务通知时间通常是：

```text
约 20 秒防抖 + 模型接口处理时间 + 本地写入和通知时间
```

网络或模型服务繁忙时可能更久，不应期待发送消息后立刻收到通知。

如果后续消息来得更晚，第一轮识别可能先创建一个暂时没有截止时间的任务。只要后续
识别仍指向同一群、同一负责人和同一事项，并引用了共同的对话证据，机器人会补全原
任务的截止时间、标题和说明，保留原任务编号，不会再创建一条重复任务。两个已经具有
不同明确截止时间的事项不会因此被自动合并。

### 5.5 识别任务的基本条件

通常需要同时具备：

1. 明确负责人；
2. 明确、可执行的动作或交付物。

截止时间可以暂时没有。没有截止时间的正式任务仍可创建，之后按群设置提醒补充截止时间。

下面通常可以识别：

```text
王政周五前完成前端页面。
李明整理本周实验数据并发给导师。
@王政 今天 21:00 前组织一次智能体方案讨论会。
张三和李四共同完成接口联调，明天下午六点前交付。
```

下面通常不会识别：

```text
今天实验结果出来了吗？
这个页面看起来还不错。
以后有空整理一下吧。
谁能帮忙看看？
王政最近是不是很忙？
```

原因是它们只是询问、评价、愿望或没有明确负责人/动作。

### 5.6 飞书 `@成员` 的作用

使用飞书真正的 `@成员` 可以提供精确的 Open ID 映射。例如：

```text
@王政 请在今天 21:00 前主持智能体讨论会议。
```

为获得最稳定的识别效果，仍建议每位成员先绑定任务姓名，并至少在群里发送过一条消息。

### 5.7 一句话包含多条任务

例如：

```text
王政周五前完成前端页面，李明周六前完成接口测试，张三今晚整理会议纪要。
```

机器人应创建三条不同任务，每条拥有独立编号、负责人、状态和提醒计划。

单次识别最多输出 10 条候选任务。会议纪要很长时，机器人会在上下文和字符预算内识别其中明确分配的事项；建议把最重要的负责人、事项和截止时间写清楚。

### 5.8 多人共同任务与多人各自任务

共同产出物：

```text
王政和李明共同完成前后端联调，周五 18:00 前交付。
```

系统创建一条共同任务，两人收到相同的任务编号。任何一名负责人完成、取消或延期这条任务，都会改变整条共同任务，并通知其他共同负责人。

独立交付物：

```text
王政和李明各自提交一份测试报告，周五前完成。
```

系统应拆成两条独立任务，分别跟踪两个人。

如果实际需要分别跟踪每个人的完成进度，请在聊天中使用“各自”“每人一份”等明确表达，不要写成“共同完成”。

### 5.9 两种任务识别范围

管理员可在“群设置”中选择：

| 模式 | 识别范围 |
| --- | --- |
| 宽泛任务（默认） | 工作、科研、会议、学习、生活需求和明确个人跑腿 |
| 仅工作 / 科研 | 工作、科研、正式项目、会议和明确交付事项；排除带饭、私人用品递送、聚餐等生活跑腿 |

宽泛模式下：

```text
王政下班前帮我带一份材料到实验室。
```

可以被视为任务。

仅工作/科研模式下，类似纯生活跑腿会被排除。两种模式都不会把没有明确负责人和动作的闲聊当成任务。

### 5.10 截止时间的理解规则

机器人使用北京时间解释日期：

- “8 月 30 日 18:00 前” → 8 月 30 日 18:00；
- “今天晚上九点前” → 当天 21:00；
- “周五前”且没有具体时分 → 周五 23:59:59；
- “两天内” → 从该消息时间精确顺延 48 小时；
- 没有明确截止日期 → 截止时间为空，不猜测日期。

相对日期依赖消息发生时刻，因此不要只写“以后”“尽快”“有空”等无法计算的表达。

### 5.11 仅识别文本消息

当前自动识别以群内普通文本消息为主。图片、文件、语音或视频本身不会被直接理解为任务内容。可以在附件旁补充一条文字说明，例如：

```text
王政，请根据上面的文件在周五前完成评审报告。
```

## 6. 置信度与待审核任务

模型会为每个候选任务给出 0%–100% 的置信度。每个群可设置“自动进入待办的置信度”，默认是 85%。

- 置信度大于或等于门槛：任务进入“待办”，立即私聊负责人并生成提醒计划。
- 置信度低于门槛：任务进入“待确认/待审核”，等待管理员判断。

待审核任务在管理员确认前：

- 不主动发送新任务通知给负责人；
- 不生成截止提醒；
- 保留识别结果和证据，供管理员查看；
- 负责人主动查询任务列表时，可能看到属于自己的“待确认”记录。

管理员在后台可以：

- 点击“确认是真实任务”，任务进入待办并开始通知、提醒；
- 点击“不是任务，撤销”，任务转为已取消并保留误识别审计。

置信度门槛不是“识别开关”。提高门槛会让更多任务进入人工审核；降低门槛会让更多任务自动进入待办。

## 7. 任务编号与状态

### 7.1 稳定任务编号

每个任务创建后都会获得稳定编号，例如：

```text
T-1A
T-3C
T-QK
```

编号由数据库任务 ID 和校验位生成：

- `T-1A` 是完整形式；
- 私聊中可以使用短形式 `1A`；
- 编号不会只限两位，任务变多后会自动增长；
- 编号不会因为改标题、改负责人或延期而变化；
- 不要自行猜编号，最好从“任务列表”或管理后台复制。

### 7.2 任务状态

| 状态 | 含义 | 是否主动提醒负责人 |
| --- | --- | --- |
| 待确认 / 待审核（pending） | 模型识别到了任务，但置信度低于门槛 | 否，管理员确认后才开始 |
| 待办（todo） | 正式开放任务，尚未完成 | 是 |
| 已逾期（overdue） | 已超过截止时间仍未完成 | 是，并通知管理员 |
| 已完成（done） | 任务已完成 | 否，未发送提醒被取消 |
| 已取消（cancelled） | 真实任务不再需要，或误识别被撤销 | 否 |
| 已合并（merged） | 此记录是重复项，已经并入另一条保留任务 | 否 |

“已合并”在数据库中保留原记录和审计，但管理页面会用独立的“已合并”文案展示。

## 8. 新任务通知

正式任务进入待办后，机器人会私聊每位负责人，例如：

```text
📌 你有一个新任务

[T-1A] 完成前端页面开发
负责人：王政
截止时间：2026-08-30 18:00

私聊机器人发送“任务列表”可查看全部任务。
也可以直接回复：T-1A 已完成。
```

多人共同任务只创建一个编号，但每位共同负责人都会收到通知。

低置信度待审核任务不会立即发送该通知。管理员确认后才会发送。

新任务通知、生命周期通知和管理员通知采用严格私聊，不会因为私聊失败自动把任务内容发到群里。

## 9. 查看任务列表

### 9.1 私聊机器人查看

私聊机器人发送以下任意一句：

```text
任务列表
本群任务
查看任务
```

普通成员默认看到自己负责的未完成任务，包括任务编号、标题、来源群、截止时间和状态。

如果发送者是一个或多个群的任务管理员，私聊列表会进入管理员视图，汇总其有权管理群里的全部未完成任务。

> 当前管理员私聊视图优先展示“所管理群的全部任务”。如果管理员还想查看自己在其他非管理群中的个人任务，可以到对应群里使用群命令查询。

### 9.2 在群里查看

群里需要显式 `@机器人`：

```text
@机器人 任务列表
@机器人 本群任务
@机器人 查看任务
```

- 普通成员只看到自己在当前群负责的未完成任务；
- 当前群管理员看到当前群所有人的未完成任务；
- 不会显示其他群的任务。

### 9.3 列表数量限制

飞书聊天回复一次最多展示 20 条任务。超过 20 条时会提示还有多少项未显示。管理员可以在管理后台使用状态筛选、编号搜索、文本搜索和每页 10 条的分页查看全部记录。

## 10. 私聊自然语言维护任务

### 10.1 基本规则

自然语言修改任务必须在私聊机器人中进行，并包含一个明确任务编号。

一条消息只能操作一个任务：

```text
T-1A 已完成
```

不要写成：

```text
T-1A 和 T-2B 都完成了
```

第二种写法会被拒绝，需要拆成两条消息。

机器人会先按任务编号做本地权限校验，再把当前消息和这一个已授权任务的摘要交给模型理解。模型不能越权选择其他群或其他人的任务。

### 10.2 负责人和管理员都能执行的操作

#### 完成任务

```text
T-1A 已完成
1A 做完了，结果已经放服务器
T-1A 已经交付，可以标记完成
```

“快完成了”“还在做”“完成了吗”不表示已经完成，不会修改状态。

完成后：

- 状态变为已完成；
- 所有未发送截止提醒被取消；
- 其他共同负责人收到状态更新；
- 本群管理员按通知设置收到私聊；
- 操作人不会再收到一条没有新增信息的自我通知。

#### 延期或设置截止时间

```text
T-1A 延期到 8 月 30 日 18:00
1A 截止时间改为下周三
T-1A 截止时间设为明天下午六点
```

延期必须给出可计算的新日期或时间。“延期一下”“以后再说”没有新时间，不会修改。

修改成功后：

- 旧的未发送提醒被取消；
- 按新截止时间重新生成提醒；
- 如果新时间已经过去，任务会进入已逾期；
- 其他共同负责人和管理员按策略收到通知。

#### 取消任务

```text
取消 T-1A，这个需求不做了
T-1A 不再需要，请取消
```

“先别急”“暂缓一下”本身不等于取消。取消表示真实任务已经不再需要继续。

取消后任务进入已取消，未发送提醒全部停止。

### 10.3 仅管理员能执行的自然语言纠错

管理员也可以私聊机器人，通过任务编号纠正任务。

#### 修改标题

```text
T-1A 的标题改为“完成移动端登录页面验收”
```

必须给出完整的新标题。

#### 修改负责人

```text
T-1A 的负责人改为王哈
T-1A 增加李明为共同负责人
T-1A 取消李明，只保留王哈
```

新负责人必须是任务来源群中已经确认身份的成员。修改后，被增加、移除和仍然负责的相关成员会收到相应私聊通知。

#### 撤销误识别

```text
T-1A 是误识别，这不是任务，请撤销
```

“撤销误识别”只用于机器人把普通聊天错误创建成任务。真实存在但后来不做的事项应使用“取消任务”。

标题、负责人和误识别纠错仅允许本群任务管理员执行。普通负责人尝试这些操作会被拒绝。

### 10.4 私聊暂不支持的操作

以下操作只在管理后台提供：

- 审核低置信度待确认任务；
- 恢复已完成或已取消任务；
- 合并重复任务；
- 手动补建完全漏识别的任务。

## 11. 私聊任务卡片

私聊机器人发送“任务列表”后，系统会优先返回交互式卡片。正式的待办和逾期任务可以直接操作：

- `✅ 完成`：把任务标记为已完成；
- `📅 选择新截止时间`：选择日期和时间后延期；
- `取消任务`：二次确认后取消任务。

待确认任务在管理员审核前不会显示这些正式生命周期按钮。

卡片操作同样会执行任务编号、当前状态、来源群和成员权限校验。卡片过旧、任务已被其他人修改或登录身份已变化时，操作可能被拒绝，此时先重新发送“任务列表”获取最新卡片。

如果飞书提示“应用未配置卡片交互功能”，说明应用部署方需要在飞书开放平台配置卡片交互回调；这不是任务数据本身的问题。

## 12. 截止时间提醒

正式待办任务设置截止时间后，系统默认规划四个阶段：

| 阶段 | 默认时间 | 接收人 |
| --- | --- | --- |
| 第一提醒 | 截止前 72 小时 | 每位负责人 |
| 第二提醒 | 截止前 24 小时 | 每位负责人 |
| 截止当天 | 截止日 09:00；如果截止时间早于 09:00，则不晚于实际截止时间 | 每位负责人 |
| 逾期提醒 | 截止后 1 分钟 | 每位负责人 |

管理员可以按群开启或关闭每个阶段，并修改时间。修改群设置后，现有未完成任务的未发送提醒会重新计算。

负责人截止提醒采用“私聊优先、群聊回退”：

1. 先私聊负责人；
2. 如果飞书明确拒绝私聊，才回到任务来源群；
3. 群聊回退时使用真正的飞书 `@负责人`；
4. 对“请求可能已经送达”的不确定错误，系统先重试，避免同一提醒私聊和群聊重复出现。

逾期后，任务状态会变为已逾期，并按当前群的管理员通知策略私聊管理员。

## 13. 没有截止时间的任务

没有明确日期不会阻止任务创建。系统默认执行一次负责人提醒和一次管理员升级：

1. 任务创建 24 小时后仍无截止时间：私聊每位负责人一次；
2. 任务创建 72 小时后仍无截止时间：私聊管理员一次；
3. 管理员也没有设置时：停止追问，视为该任务可能不需要截止日期。

机器人发送的文案会根据群设置中的真实时间显示，例如管理员把负责人提醒改成 12 小时，通知会写“已创建超过 12 小时”，不会固定写成“一天”。

管理员可以分别关闭负责人提醒或管理员升级，也可以修改两个时间。管理员升级时间必须晚于负责人提醒时间。

一旦补上截止时间：

- 尚未发送的“缺少截止时间”通知会取消；
- 开始按新的截止时间生成四阶段提醒。

## 14. 管理后台登录

管理后台只允许群任务管理员进入，不使用手工填写 Open ID 的方式登录。

管理员私聊机器人发送：

```text
管理后台
```

或：

```text
打开管理后台
```

机器人会返回私人登录链接：

- 需要在 5 分钟内打开；
- 链接只能消费一次；
- 不要转发给其他人；
- 打开页面后点击一次“进入后台”；
- 默认登录会话有效 12 小时；
- 点击左下角“管”可以退出登录。

如果链接过期、已经使用或页面停留太久，重新私聊机器人发送“管理后台”即可获得新链接。

如果一个人管理多个群，后台右上角会出现群选择器。页面只列出该用户有权管理的群，并记住最近选择的群。切换群后，任务、成员、设置和审计会全部切换到对应 `chat_id`，不会混合展示。

## 15. 管理后台：任务总览

### 15.1 统计卡片

任务总览会显示当前群的：

- 未完成任务总数；
- 7 天内截止任务数；
- 已逾期任务数；
- 缺少截止时间任务数；
- 当前群成员数；
- 本群管理员数；
- 历史任务总数。

右侧“需要关注”优先提示待审核任务，其次提示逾期任务。

### 15.2 状态筛选

支持：

- 未完成；
- 待审核；
- 待办；
- 逾期；
- 全部。

“全部”包含已完成、已取消和已合并的历史记录。

### 15.3 搜索和分页

搜索框支持：

- 完整任务编号，例如 `T-1A`；
- 短任务编号，例如 `1A`；
- 标题关键词；
- 任务说明关键词。

编号搜索只会在当前群范围内匹配，不会泄露其他群是否存在同一任务 ID。

任务列表默认每页 10 条，提供上一页、下一页、当前页、总页数和总条数。当前群、筛选条件、搜索词和页码会尽量保留，刷新页面不会无故切回另一个群或默认分组。

### 15.4 查看任务详情

点击任务后可查看：

- 稳定任务编号；
- 标题和说明；
- 主负责人和共同负责人；
- 当前状态；
- 截止时间；
- 创建方式：模型识别或管理员手动补建；
- 模型置信度；
- 证据消息；
- 生命周期记录；
- 最近提醒和通知投递记录；
- 合并后的保留任务编号。

证据消息用于解释“机器人为什么创建了这项任务”。管理员手动创建的任务没有模型证据消息，会明确显示为“管理员手动补建”。

## 16. 管理后台：创建和维护任务

### 16.1 手动补建漏识别任务

当群聊任务完全漏识别、数据库里没有记录时，点击“＋ 新建任务”。

需要填写：

- 任务标题：必填，1–200 个字符；
- 任务说明：选填，最多 2,000 个字符；
- 截止时间：选填，北京时间；
- 负责人：至少 1 人，最多 20 人。

负责人只能从当前群中已绑定任务姓名的有效成员里选择。选择顺序决定负责人顺序：

- 第一位是主负责人；
- 后续成员是共同负责人。

创建成功后：

- 直接进入待办，不经过模型审核；
- 生成稳定任务编号；
- 私聊每位负责人；
- 有截止时间则生成提醒计划；
- 无截止时间则进入缺少截止时间规则；
- 保存“管理员手动补建”的审计来源。

### 16.2 审核待确认任务

打开待审核任务后可选择：

- “确认是真实任务”：进入待办，通知负责人并生成提醒；
- “不是任务，撤销”：标记为误识别，保留审计但不打扰负责人。

### 16.3 修改标题

正式待办或逾期任务可以修改标题，长度为 1–200 个字符。修改会写入生命周期审计，并通知相关负责人和其他管理员。

### 16.4 修改负责人

可以从当前有效群成员中选择 1–20 名负责人。未绑定任务姓名的成员不可选择。

修改后：

- 新增负责人收到“你已被设为负责人”；
- 被移除成员收到“你已不再负责”；
- 仍然负责的共同成员收到负责人列表变化；
- 提醒计划按当前负责人重新同步；
- 管理员收到纠错通知，操作人本身不会重复收到无新增信息的通知。

### 16.5 修改截止时间

正式待办或逾期任务可以选择新的北京时间。保存后，旧未发送提醒取消，新提醒重新生成。

当前后台提供“修改为一个新时间”，不提供直接清空已有截止时间的按钮。

### 16.6 完成、取消和撤销误识别

管理员可以在任务详情中：

- 标记完成；
- 取消真实任务；
- 撤销误识别。

三个操作都会要求二次确认、写入审计并同步停止未发送提醒。请根据业务含义选择“取消”或“误识别”，不要混用。

### 16.7 恢复已完成或已取消任务

如果误点完成或取消，管理员可点击“恢复为开放任务”。

恢复后：

- 原完成或取消记录不删除；
- 截止时间仍在未来或为空时恢复为待办；
- 截止时间已经过去时恢复为已逾期；
- 按当前截止时间重建提醒；
- 通知负责人和其他管理员。

恢复只在管理后台提供，普通私聊和任务卡片不能触发。

### 16.8 合并重复任务

同一事项被重复识别时，管理员可选择一条任务作为“保留任务”，把另一条合并进去。

合并会：

- 保留目标任务编号作为正式编号；
- 原重复任务显示为已合并，不物理删除；
- 把证据消息和检测来源并入保留任务；
- 取消重复任务尚未发送的提醒和通知；
- 保留合并审计；
- 不再发送一条没有意义的“合并状态通知”。

只能合并同一群中的有效任务，不能跨群合并。

## 17. 管理后台：群管理员

“群管理员”页面显示当前飞书群成员和管理员审计。

管理员可以：

- 把当前群成员设为任务管理员；
- 撤销其他任务管理员；
- 查看飞书名称和任务姓名；
- 查看谁是当前群主；
- 查看授权、撤权和自动回收记录。

规则：

- 只有当前飞书群成员可以成为管理员；
- 至少保留一名管理员，最后一名不能被撤销；
- 成员离群后自动撤权；
- 如果离群成员不再管理任何其他群，其后台会话也会失效；
- 群主可随时通过 `@机器人 接管本群` 恢复自己的任务管理权限。

多管理员群默认把管理员通知发给全部管理员。管理员也可以在群设置中改为只通知指定的一名或多名管理员。

## 18. 管理后台：群设置

所有设置只作用于当前选中的群。

### 18.1 自动任务识别

关闭后：

- 新群消息仍然保存；
- 新消息不再进入任务识别队列；
- 不会因为这些新消息自动创建任务；
- 已有任务、提醒和历史记录不受影响。

关闭识别时，任务范围和置信度控件会不可编辑。重新开启后才可以调整并保存。

### 18.2 任务识别范围

选择“宽泛任务”或“仅工作 / 科研”，具体含义见第 5.9 节。默认是宽泛任务。

### 18.3 自动进入待办的置信度

范围为 0%–100%，页面按 5% 步长调整，默认 85%。只有自动识别开启时可以调整。

### 18.4 负责人截止提醒

四个阶段可以独立开关：

- 第一提醒；
- 第二提醒；
- 截止当天；
- 任务逾期。

关闭某阶段后，对应的未发送提醒会取消，该阶段的时间输入也会禁用。

可设置范围：

| 设置 | 范围 |
| --- | --- |
| 第一提醒 | 截止前 2–720 小时 |
| 第二提醒 | 截止前 1–719 小时 |
| 截止当天 | 0–23 时整点 |
| 逾期提醒 | 截止后 0–1,440 分钟 |

第一提醒必须早于第二提醒，也就是第一提醒的“提前小时数”必须更大。

### 18.5 未设置截止时间

可以独立控制：

- 是否提醒负责人；
- 是否升级管理员；
- 创建后多少小时提醒负责人；
- 创建后多少小时升级管理员。

负责人范围为 1–720 小时，管理员范围为 2–2,160 小时，管理员升级必须晚于负责人提醒。两个阶段最多各发送一次，之后停止追问。

### 18.6 管理员通知对象

可选择：

- 全部管理员：默认，向当前群全部管理员发送；
- 指定管理员：选择一名或多名当前群管理员。

该范围用于任务完成、取消、延期、逾期、纠错和缺少截止时间等管理员私聊通知。被撤销权限或离群的成员会从有效通知对象中移除。

### 18.7 设置审计

每次保存群设置都会记录：

- 操作人；
- 修改时间；
- 修改前值；
- 修改后值。

群时区固定显示为 `Asia/Shanghai`，当前不提供修改入口。

## 19. 管理员通知与共同负责人通知

以下变化会产生私聊通知：

| 事件 | 可能的接收人 |
| --- | --- |
| 新任务创建或待确认任务获批 | 每位负责人 |
| 完成、取消、延期 | 其他共同负责人、全部或指定管理员 |
| 任务逾期 | 负责人、全部或指定管理员 |
| 缺少截止时间 | 负责人一次，之后管理员一次 |
| 修改标题 | 负责人、其他管理员 |
| 修改负责人 | 新增、移除、仍负责成员和其他管理员 |
| 撤销误识别 | 原负责人和其他管理员 |
| 恢复任务 | 负责人和其他管理员 |

系统会跳过操作人自己的重复通知，也会避免同一人因为同时是负责人和管理员而收到内容相同的两份通知。

## 20. 多群使用与数据隔离

机器人可以加入多个群。系统以飞书 `chat_id` 作为隔离边界：

- 每个群独立初始化；
- 姓名绑定独立；
- 管理员独立；
- 识别开关和置信度独立；
- 提醒时间和通知对象独立；
- 任务和证据消息独立；
- 群内查询只查询当前群；
- 管理后台只能切换到当前登录人有权管理的群；
- 跨群编号搜索不会泄露其他群任务是否存在。

如果群聊改名，系统会从飞书刷新群名。刷新失败时会暂时隐藏群名，而不是继续显示一个已知过期的旧名称。任务本身仍由不可变的 `chat_id` 关联，不会因改名丢失。

## 21. 推荐使用流程

### 21.1 新群上线清单

1. 把机器人加入群。
2. 当前群主发送 `@机器人 初始化本群`。
3. 每位成员发送 `@机器人 绑定姓名：真实姓名`。
4. 群主私聊机器人发送“管理后台”。
5. 在后台添加其他管理员。
6. 确认识别范围、置信度和提醒规则。
7. 发一条明确测试任务，确认负责人收到私聊。
8. 负责人用任务编号完成测试任务。

### 21.2 普通负责人日常流程

1. 收到新任务私聊。
2. 私聊发送“任务列表”查看未完成任务。
3. 工作完成后回复 `T-编号 已完成`，或点击卡片完成。
4. 需要延期时明确给出新时间。
5. 任务不再需要时明确取消，不要用“先别急”等含糊表达。

### 21.3 管理员日常流程

1. 定期进入后台查看“待审核”和“逾期”。
2. 审核低置信度识别结果。
3. 对漏识别任务手动补建。
4. 对错误标题、负责人和截止时间及时纠正。
5. 对重复任务执行合并。
6. 根据群使用习惯调整提醒规则和通知对象。

## 22. 完整命令速查表

### 22.1 群聊命令

| 发送人 | 命令 | 作用 |
| --- | --- | --- |
| 当前群主 | `@机器人 初始化本群` | 第一次启用本群任务管理 |
| 当前群主 | `@机器人 接管本群` | 恢复当前群主的任务管理员权限 |
| 任意成员 | `@机器人 绑定姓名：王政` | 绑定或更换当前群任务姓名 |
| 任意成员 | `@机器人 我的姓名` | 查询当前群绑定姓名 |
| 任意成员 | `@机器人 任务列表` | 普通成员看自己的任务，管理员看本群全部任务 |
| 身份管理员 | `@机器人 绑定成员 @成员 为 王政` | 替其他成员绑定姓名；普通群管理员不一定有此权限 |

### 22.2 私聊命令

| 发送人 | 命令示例 | 作用 |
| --- | --- | --- |
| 任意已登记成员 | `任务列表` | 查看有权限的未完成任务 |
| 任意已登记成员 | `查看任务` | 同上 |
| 负责人或管理员 | `T-1A 已完成` | 完成一项任务 |
| 负责人或管理员 | `T-1A 延期到 8 月 30 日 18:00` | 修改截止时间 |
| 负责人或管理员 | `取消 T-1A` | 取消真实任务 |
| 管理员 | `T-1A 标题改为……` | 修改标题 |
| 管理员 | `T-1A 负责人改为王哈` | 修改负责人 |
| 管理员 | `T-1A 不是任务，请撤销` | 撤销误识别 |
| 管理员 | `管理后台` | 获取 5 分钟内有效的一次性后台链接 |

## 23. 常见问题与排查

### 23.1 群主发送“初始化本群”没有回复

检查：

1. 是否真的用飞书 `@` 选择了机器人；
2. 发送人是否为当前飞书群主；
3. 机器人是否仍在群里；
4. 应用是否已经发布并具有群消息、发消息和成员目录权限；
5. 后端 Listener 是否在线。

如果机器人回复“暂时无法核验本群群主”，稍后重试，不要反复初始化。

### 23.2 发了明确任务却没有创建

依次检查：

1. 本群是否已经初始化；
2. 群设置中的“自动任务识别”是否开启；
3. 消息是否是普通文本；
4. 负责人是否明确，是否已经绑定姓名或被正确 `@`；
5. 事项是否可执行，而不是询问或愿望；
6. 是否等待了至少约 20 秒加模型处理时间；
7. 是否进入了后台“待审核”，而不是直接待办；
8. 模型服务和识别 Worker 是否在线。

如果数据库中完全没有任务，管理员可在后台手动补建。

### 23.3 为什么识别不是立即完成

系统故意等待约 20 秒收集上下文，随后还需要调用模型、做本地严格校验、写数据库并排队发送通知。连续聊天会延长防抖窗口，这是正常行为。

### 23.4 为什么没有收到新任务私聊

可能原因：

- 任务进入了待审核，尚未正式确认；
- 负责人姓名没有正确绑定；
- 负责人已经离群；
- 飞书拒绝应用私聊；
- 任务通知 Worker 未运行；
- 通知正在失败重试。

管理员可以在任务详情的“提醒与通知”中查看投递记录。

### 23.5 为什么截止提醒出现在群里

截止提醒采用私聊优先。如果明确无法私聊负责人，系统才会回退到来源群并 `@负责人`。新任务通知和生命周期通知不会进行这种群聊回退。

### 23.6 为什么“任务列表”显示待确认任务，但没有新任务通知

待确认任务已经作为识别记录保存，所以主动查询时可能看见；但它尚未成为正式待办，因此不会主动通知和提醒。等待管理员审核即可。

### 23.7 为什么置信度滑块不能修改

当“自动任务识别”关闭时，识别范围和置信度都被禁用。先开启自动识别，再调整滑块并保存群设置。

### 23.8 管理后台链接失效

一次性链接可能已经超过 5 分钟、已被消费或被转发打开。回到飞书私聊重新发送“管理后台”。

### 23.9 为什么重新绑定姓名后旧姓名仍不可用

正常情况下，同一成员改名后旧姓名立即释放。如果仍不可用：

- 确认改名命令是在同一个群发送；
- 使用 `@机器人 我的姓名` 检查当前绑定；
- 确认旧姓名不是被另一个成员占用；
- 让管理员刷新成员页面，触发最新群成员同步。

### 23.10 成员已经离群，为什么姓名还没有释放

需要系统完成一次权威群成员同步。管理员打开后台成员页面、群主执行需要成员核验的命令，或等待下一次目录刷新后，离群成员的姓名和管理员权限会被清理。

### 23.11 群改名后为什么页面还是旧名称

群名会通过飞书目录刷新，不参与任务隔离。短时间缓存或飞书接口失败可能导致更新稍有延迟；刷新成功后会显示新名称，刷新失败时系统倾向于隐藏名称而不是长期显示旧值。

### 23.12 多名管理员都会收到通知吗

默认会通知当前群全部管理员，但会跳过操作人自己和已经以共同负责人身份收到同类信息的人。管理员也可以在群设置中改成只通知指定管理员。

### 23.13 为什么管理员私聊“任务列表”看不到自己在另一个群的个人任务

管理员私聊默认进入“管理员视图”，范围是自己有权管理的群。去另一个群发送 `@机器人 任务列表`，可以查看自己在该群的个人任务。

### 23.14 输入短编号为什么提示编号无效

编号带校验位，输错一个字符就会被拒绝。请从任务卡片或管理后台复制编号。短编号支持 `1A`，完整编号支持 `T-1A`。

### 23.15 一条消息能同时完成多个任务吗

不能。私聊生命周期操作一次只允许一个有效任务编号，目的是防止模型误改多条任务。请分别发送。

### 23.16 共同任务可以一个人先完成自己的部分吗

当前共同任务是一个共享状态，任何负责人点击完成都会完成整条任务。如果需要分别跟踪个人进度，应在创建时拆成多条独立任务，或由管理员在后台调整任务结构。

## 24. 使用边界

当前版本的主要边界：

- 自动识别只处理群内文本消息，不直接理解附件、图片、音视频内容；
- 私聊自然语言修改必须带一个有效任务编号；
- 一条私聊消息只能修改一项任务；
- 普通负责人不能修改标题、负责人或执行误识别撤销；
- 恢复任务、合并任务和低置信度审核只在管理后台提供；
- 已有截止时间当前不能在后台直接清空，只能改为另一个时间；
- 飞书聊天任务列表一次最多展示 20 条，完整历史请使用后台；
- 系统固定使用中国标准时间；
- 自动识别依赖模型服务，模型可能漏识别或误识别，因此保留了人工审核、补建、纠错和合并工具。

## 25. 数据与隐私说明

为了实现上下文识别、任务查询、提醒和审计，系统会保存或处理：

- 群 ID 和群名称；
- 成员飞书 Open ID、飞书名称和绑定的任务姓名；
- 机器人可见范围内的群消息文本和时间；
- 任务标题、说明、负责人、截止时间、状态和证据消息；
- 提醒、通知、管理员变更、设置修改和任务生命周期记录。

开启自动任务识别时，最近群聊上下文、成员姓名和 Open ID 会按部署方配置发送到任务识别模型接口。模型返回结果还会经过本地 JSON 契约、群成员、Open ID、姓名、证据消息和权限校验，不能仅凭模型输出直接越权写入其他群任务。

管理者应根据所在组织的隐私、数据合规和成员告知要求使用机器人。不要在群里发送本来就不应该被机器人、应用服务或模型接口处理的敏感信息。

## 26. 最简使用记忆

如果只记住六件事：

1. 新群先由群主发送 `@机器人 初始化本群`。
2. 每位成员发送 `@机器人 绑定姓名：真实姓名`。
3. 普通任务安排直接在群里说，不需要 `@机器人`。
4. 私聊发送“任务列表”查看编号。
5. 私聊发送 `T-编号 已完成/延期到…/取消` 维护任务。
6. 管理员私聊发送“管理后台”审核和纠错。
